# 北线灯具 · 客户预览

Vite + React 构建产物示例。线上：https://example-vite.demox.site

```bash
# 可复用主站 node_modules，不必再装一份
ln -sfn ../../node_modules node_modules
../../node_modules/.bin/vite build
rm -f node_modules

demox deploy ./dist --id TKEMDUR9
```
